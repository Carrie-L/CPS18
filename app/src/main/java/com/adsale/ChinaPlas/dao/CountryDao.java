package com.adsale.ChinaPlas.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "COUNTRY".
*/
public class CountryDao extends AbstractDao<Country, String> {

    public static final String TABLENAME = "COUNTRY";

    /**
     * Properties of entity Country.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property CountryID = new Property(0, String.class, "CountryID", true, "COUNTRY_ID");
        public final static Property CountryNameTW = new Property(1, String.class, "CountryNameTW", false, "COUNTRY_NAME_TW");
        public final static Property CountryNameCN = new Property(2, String.class, "CountryNameCN", false, "COUNTRY_NAME_CN");
        public final static Property CountryNameEN = new Property(3, String.class, "CountryNameEN", false, "COUNTRY_NAME_EN");
        public final static Property SortTW = new Property(4, String.class, "SortTW", false, "SORT_TW");
        public final static Property SortCN = new Property(5, String.class, "SortCN", false, "SORT_CN");
        public final static Property SortEN = new Property(6, String.class, "SortEN", false, "SORT_EN");
    }

    public CountryDao(DaoConfig config) {
        super(config);
    }
    
    public CountryDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"COUNTRY\" (" + //
                "\"COUNTRY_ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: CountryID
                "\"COUNTRY_NAME_TW\" TEXT NOT NULL ," + // 1: CountryNameTW
                "\"COUNTRY_NAME_CN\" TEXT NOT NULL ," + // 2: CountryNameCN
                "\"COUNTRY_NAME_EN\" TEXT NOT NULL ," + // 3: CountryNameEN
                "\"SORT_TW\" TEXT NOT NULL ," + // 4: SortTW
                "\"SORT_CN\" TEXT NOT NULL ," + // 5:  SortCN
                "\"SORT_EN\" TEXT NOT NULL );"); // 6: SortEN
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"COUNTRY\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Country entity) {
        stmt.clearBindings();
 
        String CountryID = entity.getCountryID();
        if (CountryID != null) {
            stmt.bindString(1, CountryID);
        }
        stmt.bindString(2, entity.getCountryNameTW());
        stmt.bindString(3, entity.getCountryNameCN());
        stmt.bindString(4, entity.getCountryNameEN());
        stmt.bindString(5, entity.getSortTW());
        stmt.bindString(6, entity.getSortCN());
        stmt.bindString(7, entity.getSortEN());
    }

    /** @inheritdoc */
    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Country readEntity(Cursor cursor, int offset) {
        Country entity = new Country( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // CountryID
            cursor.getString(offset + 1), // CountryNameTW
            cursor.getString(offset + 2), // CountryNameCN
            cursor.getString(offset + 3), // CountryNameEN
            cursor.getString(offset + 4), // SortTW
            cursor.getString(offset + 5), // SortCN
            cursor.getString(offset + 6) // SortEN
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Country entity, int offset) {
        entity.setCountryID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setCountryNameTW(cursor.getString(offset + 1));
        entity.setCountryNameCN(cursor.getString(offset + 2));
        entity.setCountryNameEN(cursor.getString(offset + 3));
        entity.setSortTW(cursor.getString(offset + 4));
        entity.setSortCN(cursor.getString(offset + 5));
        entity.setSortEN(cursor.getString(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected String updateKeyAfterInsert(Country entity, long rowId) {
        return entity.getCountryID();
    }
    
    /** @inheritdoc */
    @Override
    public String getKey(Country entity) {
        if(entity != null) {
            return entity.getCountryID();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
