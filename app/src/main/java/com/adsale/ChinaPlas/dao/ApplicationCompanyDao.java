package com.adsale.ChinaPlas.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.adsale.ChinaPlas.dao.ApplicationCompany;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "APPLICATION_COMPANY".
*/
public class ApplicationCompanyDao extends AbstractDao<ApplicationCompany, Void> {

    public static final String TABLENAME = "APPLICATION_COMPANY";

    /**
     * Properties of entity ApplicationCompany.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property CompanyID = new Property(0, String.class, "CompanyID", false, "COMPANY_ID");
        public final static Property IndustryID = new Property(1, String.class, "IndustryID", false, "INDUSTRY_ID");
    };


    public ApplicationCompanyDao(DaoConfig config) {
        super(config);
    }
    
    public ApplicationCompanyDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"APPLICATION_COMPANY\" (" + //
                "\"COMPANY_ID\" TEXT NOT NULL ," + // 0: CompanyID
                "\"INDUSTRY_ID\" TEXT NOT NULL );"); // 1: IndustryID
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"APPLICATION_COMPANY\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ApplicationCompany entity) {
        stmt.clearBindings();
        stmt.bindString(1, entity.getCompanyID());
        stmt.bindString(2, entity.getIndustryID());
    }

    /** @inheritdoc */
    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    /** @inheritdoc */
    @Override
    public ApplicationCompany readEntity(Cursor cursor, int offset) {
        ApplicationCompany entity = new ApplicationCompany( //
            cursor.getString(offset + 0), // CompanyID
            cursor.getString(offset + 1) // IndustryID
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ApplicationCompany entity, int offset) {
        entity.setCompanyID(cursor.getString(offset + 0));
        entity.setIndustryID(cursor.getString(offset + 1));
     }
    
    /** @inheritdoc */
    @Override
    protected Void updateKeyAfterInsert(ApplicationCompany entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    /** @inheritdoc */
    @Override
    public Void getKey(ApplicationCompany entity) {
        return null;
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
