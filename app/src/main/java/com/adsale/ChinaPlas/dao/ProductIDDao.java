package com.adsale.ChinaPlas.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.adsale.ChinaPlas.dao.ProductID;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PRODUCT_ID".
*/
public class ProductIDDao extends AbstractDao<ProductID, String> {

    public static final String TABLENAME = "PRODUCT_ID";

    /**
     * Properties of entity ProductID.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property IndustryID = new Property(0, String.class, "IndustryID", true, "INDUSTRY_ID");
        public final static Property IndustryIdParent = new Property(1, String.class, "IndustryIdParent", false, "INDUSTRY_ID_PARENT");
    };


    public ProductIDDao(DaoConfig config) {
        super(config);
    }
    
    public ProductIDDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PRODUCT_ID\" (" + //
                "\"INDUSTRY_ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: IndustryID
                "\"INDUSTRY_ID_PARENT\" TEXT NOT NULL );"); // 1: IndustryIdParent
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PRODUCT_ID\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ProductID entity) {
        stmt.clearBindings();
 
        String IndustryID = entity.getIndustryID();
        if (IndustryID != null) {
            stmt.bindString(1, IndustryID);
        }
        stmt.bindString(2, entity.getIndustryIdParent());
    }

    /** @inheritdoc */
    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ProductID readEntity(Cursor cursor, int offset) {
        ProductID entity = new ProductID( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // IndustryID
            cursor.getString(offset + 1) // IndustryIdParent
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ProductID entity, int offset) {
        entity.setIndustryID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setIndustryIdParent(cursor.getString(offset + 1));
     }
    
    /** @inheritdoc */
    @Override
    protected String updateKeyAfterInsert(ProductID entity, long rowId) {
        return entity.getIndustryID();
    }
    
    /** @inheritdoc */
    @Override
    public String getKey(ProductID entity) {
        if(entity != null) {
            return entity.getIndustryID();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
