<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="exhibitorModel"
            type="com.adsale.ChinaPlas.viewmodel.ExhibitorViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <EditText
            android:id="@+id/editFilter"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_alignParentTop="true"
            android:background="@drawable/filter_bg"
            android:hint="@string/editFilter_hint"
            android:padding="10dp"
            android:singleLine="true"
            android:text="@{exhibitorModel.etFilter}"
            />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_ks"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/editFilter"
            app:items="@{exhibitorModel.exhibitors}"
            />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_side_bar"
            android:layout_width="32dp"
            android:layout_height="match_parent"
            android:layout_below="@id/editFilter"
            android:background="@drawable/scrollbar_bg"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            app:items="@{exhibitorModel.letters}"
            />

        <TextView
            android:id="@+id/DialogText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_gravity="center"
            android:background="@drawable/show_head_toast_bg"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:text="@{exhibitorModel.indexText}"
            />

        <TextView
            style="@style/no_data_style"
            android:visibility="@{exhibitorModel.noData?View.VISIBLE:View.INVISIBLE}" />

    </RelativeLayout>

</layout>